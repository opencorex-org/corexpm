# Examples

Examples will demonstrate supported CorexPM behavior with minimal JavaScript
projects. Until `install` works, documentation uses conceptual examples rather
than checked-in generated `node_modules` trees or fabricated lockfiles.

