# Applications

Planned repository applications are the documentation site, compatibility
playground, and benchmark dashboard. The Markdown under `docs/` remains the
source of truth; a site may render it later without forking specifications.

